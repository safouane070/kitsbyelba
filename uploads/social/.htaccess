# Alleen media serveren — nooit scripts uitvoeren vanuit deze map (defense in depth).
<FilesMatch "\.(php|phtml|phar|php[0-9]|pl|py|cgi|sh|asp|aspx)$">
    Require all denied
</FilesMatch>
php_flag engine off
